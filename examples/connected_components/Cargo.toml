[package]
name = "connected-components-example"
version = "0.1.0"
edition = "2021"

[dependencies]
pregel-sdk = { path = "../../crates/pregel-sdk" }
pregel-common = { path = "../../crates/pregel-common" }
