[package]
name = "pregel-wasm-algos"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pregel-common = { path = "../pregel-common" }
bincode = "1"
serde = { version = "1", features = ["derive"] }
