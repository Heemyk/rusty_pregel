[package]
name = "pregel-messaging"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = "1"
pregel-common = { path = "../pregel-common" }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["sync"] }
