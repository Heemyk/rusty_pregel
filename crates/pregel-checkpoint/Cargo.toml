[package]
name = "pregel-checkpoint"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = "1"
pregel-common = { path = "../pregel-common" }
pregel-storage = { path = "../pregel-storage" }
serde = { version = "1", features = ["derive"] }
